name: Vale
on: [pull_request]  # yamllint disable-line rule:truthy

jobs:
  vale:
    name: Run vale on the book
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: vale-cli/vale-action@latest
        with:
          version: 3.15.1
