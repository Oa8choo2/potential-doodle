name: Vale
on: [pull_request]

jobs:
  vale:
    name: Run vale on the book
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkoutv4
      - uses: errata-ai/vale-action@2.1.1
        with:
          version: 3.10.0
